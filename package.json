{
  "name": "incidentmind-v2",
  "private": true,
  "version": "0.0.0",
  "description": "STAGING MANIFEST - P2 local workspace only. P1 owns the real root package.json; discard this file when the repo arrives.",
  "scripts": {
    "control": "node control/server.js",
    "dev-windows": "node tools/dev-windows.js --dev"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ws": "^8.18.3"
  }
}
